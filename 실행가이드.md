# HOLMZ 실행 가이드

매장 PC에 HOLMZ 근로 종합 관리 프로그램을 설치하고 운영하는 방법을 정리한 문서입니다. (개발·빌드 정보는 `README.md` 참고)

---

## 1. 설치 및 첫 실행

**필요 환경**: Windows 10 / 11 (64bit), 인터넷 연결

1. `build/holmz.exe` 파일을 매장 PC의 원하는 폴더로 복사합니다 (예: `C:\HOLMZ\holmz.exe`).
   - 별도 설치 과정이 없는 단일 실행 파일입니다. **한번 위치를 정하면 옮기지 마세요** — 작업 스케줄러가 이 경로를 기억합니다.
2. 더블클릭으로 실행합니다.
3. 화면이 뜨지 않고 오류가 나면 **WebView2 런타임**이 없는 PC입니다. [Microsoft WebView2 다운로드](https://developer.microsoft.com/microsoft-edge/webview2/) 페이지에서 "에버그린 부트스트래퍼"를 설치한 뒤 다시 실행하세요. (2022년 이후 업데이트된 Windows 10/11에는 기본 내장)

> 데이터는 `%APPDATA%\HOLMZ\` 폴더에 저장됩니다 (`holmz.db`, 사진, 암호화 토큰). PC를 바꾸거나 백업할 때 이 폴더를 통째로 복사하면 됩니다.

## 2. 최초 설정 (관리자, 한 번만)

왼쪽 메뉴의 **관리자** 구역에서 순서대로 진행합니다.

| 순서 | 화면 | 할 일 |
|---|---|---|
| 1 | 직원 관리 | 근무자 **이름·학번·학과** 등록 (학번은 본인 확인에 사용) |
| 2 | 체크리스트 관리 | 오픈용/마감용 점검 항목 등록 (항목명·순서·필수 여부) |
| 3 | 설정 (Google 연동) | 관리자 PIN, **공지사항**(출근 시 팝업), **업무 항목**(정각 기록용), Google 계정 인증 *(선택 — 3장 참고)* |
| 4 | 스케줄 관리 | 오픈/마감 시각 입력 후 **[템플릿 적용]** *(선택 — 4장 참고)* |
| 5 | 근로 스케줄 | 직원별 주간 근무 배치(정시 단위)와 휴가·대타 관리 |
| 6 | 영상 재생 | 매장에서 틀 YouTube 영상 URL 등록 *(선택)* |

## 3. Google Drive 연동 (선택)

근로기록·체크리스트를 Google 스프레드시트로 자동 백업하려면:

1. [Google Cloud Console](https://console.cloud.google.com)에서 프로젝트를 만들고 **Drive API**와 **Sheets API**를 활성화합니다.
2. OAuth 동의 화면을 구성한 뒤 **"데스크톱 앱" 유형의 OAuth 클라이언트 ID**를 생성합니다.
3. 내려받은 JSON 파일을 `%APPDATA%\HOLMZ\credentials.json` 이름으로 저장합니다.
4. 앱의 **설정 (Google 연동)** → **[Google 계정 인증]** 클릭 → 브라우저에서 매장 Google 계정으로 승인합니다.
5. **[지금 동기화]**로 동작을 확인합니다. 결과는 Drive의 **"HOLMZ 근로기록"** 폴더에 날짜별 시트로 쌓입니다.

- 동기화 대상은 **퇴근 처리까지 끝난** 기록입니다. 마감 스케줄을 등록해두면 매일 자동 업로드됩니다.
- 동기화할 때마다 **직원 명단·주간 근무 스케줄·예외(휴가/대타)**도 같은 폴더의 "HOLMZ_직원·근무스케줄" 시트로 함께 갱신됩니다.
- 인증 토큰은 Windows 계정에 묶여 암호화 저장되므로(`token.enc`) 파일을 복사해가도 다른 PC에서는 열리지 않습니다. PC를 교체하면 재인증하세요.

## 4. 자동화 스케줄 (선택)

**스케줄 관리** 화면에서 오픈 시각(예 09:00)·마감 시각(예 22:00)을 넣고 **[템플릿 적용]**을 누르면 다음 5개 작업이 Windows 작업 스케줄러에 등록됩니다:

- 오픈 시각 — 오픈 체크리스트 알림, 영상 재생 시작
- 마감 시각 — 마감 체크리스트 알림, 근로기록 업로드, 영상 재생 종료

개별 작업도 직접 추가할 수 있습니다(요일 미선택 시 매일 실행). 동작으로 **"음성 재생 (안내방송)"**을 선택하면 지정한 음성 파일(mp3/wav/m4a/ogg)을 정해진 시각에 **Windows Media Player가 직접 재생**합니다 — HOLMZ 앱이 꺼져 있어도 동작하며, 재생이 끝나면 플레이어 창은 자동으로 닫힙니다. 예: 마감 30분 전/10분 전 안내방송. [🔊 음성 파일 선택]으로 파일을 고르고, **재생 횟수(1~5회)**를 지정하면 같은 방송이 연속으로 반복 재생됩니다. 등록 후 [▶ 테스트]로 미리 들어볼 수 있습니다. 음성 파일은 경로가 바뀌면 재생되지 않으니 고정 폴더(예: `C:\HOLMZ\audio\`)에 두세요. **등록 시 관리자 권한(UAC) 창이 뜰 수 있습니다 — 허용해주세요.** 등록된 작업은 Windows 작업 스케줄러의 `HOLMZ` 폴더에서도 확인됩니다.

> 절전 모드에서도 실행하려면: 작업 스케줄러 앱 → HOLMZ 폴더 → 각 작업 속성 → 조건 탭 → "이 작업을 실행하기 위해 절전 모드 종료" 체크.

**영상 재생은 별도 팝업 창으로 나옵니다.** 재생을 시작하면(수동 또는 스케줄) 전용 재생 창이 열립니다 — 이 창을 TV/보조 모니터로 옮기고 **더블클릭**하면 전체화면이 됩니다. 재생 중에도 HOLMZ 본 화면에서 체크리스트·근로기록 등 모든 기능을 자유롭게 쓸 수 있습니다. 재생 창을 실수로 닫아도 워치독이 자동으로 다시 열며, 완전히 끄려면 영상 재생 화면의 [재생 종료]를 누르세요.

## 5. 매일 사용법 (근무자)

1. **접속**: 앱을 켜면 접속 화면이 나옵니다 — **이름과 학번**을 입력해 로그인 (관리자는 관리자 이름 + PIN, 초기 계정 admin / 0000000000 — 반드시 변경). 직원으로 접속하면 본인 화면만 표시됩니다.
2. **출근**: **근로기록** 화면 → **[출근]** 버튼 → **공지사항 팝업** 확인
3. **오픈 체크리스트**: 항목을 확인하며 체크, 필요하면 **[📷 사진]**으로 청소·재고 상태 첨부 → 필수 항목을 모두 체크하면 **[오픈 완료 처리]**
4. **근무 중**: **매 정각에 업무 기록 알림**이 뜹니다 — 목록에서 지금 하는 업무를 선택하면 `[14:00] 청소` 형식으로 자동 기록됩니다. 알림을 놓쳤다면 근로기록 화면의 **"놓친 정각 기록"**에서 시각·업무를 골라 기입할 수 있습니다. 그 외 업무는 수시로 직접 입력 → **[기록]**
5. **안내 방송**: **안내 방송** 메뉴에서 문구를 입력하고 [📢 방송 시작]을 누르면 즉시 스피커로 송출됩니다 (한국어/영어 자동 인식, 속도 조절, 최근 방송 다시 틀기). 처음 쓰는 문구는 음성 생성에 15~20초가 걸리고, 같은 문구는 이후 즉시 재생됩니다. 음성 엔진(MeloTTS)은 `holmz.exe` 옆 `tts` 폴더에 함께 배포되며 Python·인터넷 설치가 필요 없습니다 — 이 폴더를 지우면 Windows 내장 음성으로 대체됩니다. 만든 안내문은 화면 아래 **생성한 안내문** 목록에 남아 [다시 방송]으로 즉시 재사용하거나 [삭제]로 정리할 수 있습니다.
6. **마감**: 마감 체크리스트 완료 처리 → **[퇴근]** 버튼 (확인 후 근무시간 자동 계산)

## 6. 상시 실행과 종료

- 창의 **X 버튼을 눌러도 종료되지 않고** 트레이(작업 표시줄 오른쪽 아이콘)로 숨습니다. 스케줄 알림이 오면 창이 자동으로 다시 나타납니다.
- 트레이 아이콘 메뉴: **HOLMZ 열기** / **지금 동기화** / **종료**
- 완전히 종료하려면 트레이 메뉴의 **[종료]**를 사용하세요.

## 7. 문제 해결

| 증상 | 조치 |
|---|---|
| 실행해도 창이 안 뜸 | WebView2 런타임 설치 (1장). 이미 실행 중이면 트레이 아이콘 → HOLMZ 열기 |
| 스케줄 등록 실패 | 앱을 마우스 오른쪽 → "관리자 권한으로 실행" 후 다시 등록 |
| Google 인증 실패 | `%APPDATA%\HOLMZ\credentials.json` 존재·파일명 확인, "데스크톱 앱" 유형인지 확인 |
| 동기화 0건 | 퇴근 처리된 기록만 업로드됩니다. 퇴근 버튼을 눌렀는지 확인 |
| 영상이 재생되지 않음 | 재생목록에 영상이 있는지, 인터넷 연결 확인. 일시 오류는 워치독이 자동 재시작하며(재생 창을 실수로 닫아도 다시 열림), 반복 실패 시 화면에 경고가 표시됩니다 |
| 학번/PIN 문제 | 직원 학번: 관리자가 직원 관리에서 바로 수정 가능. 관리자 PIN 분실 시 `%APPDATA%\HOLMZ\holmz.db` 삭제 외 복구 수단이 없으니 기록해두세요 |
| 데이터 백업 | `%APPDATA%\HOLMZ\` 폴더 복사 (앱 종료 후) |

## 8. 참고

- 매장에서의 YouTube 상시 재생은 YouTube 약관상 제한될 수 있습니다. 자체 제작 콘텐츠 또는 정식 라이선스가 있는 소스 사용을 권장합니다.
- 근로기록에는 개인정보(근무시간 등)가 포함됩니다. 관리자 PIN을 설정해 접근을 제한하고, 보관 기간 정책을 정해 운영하세요.
